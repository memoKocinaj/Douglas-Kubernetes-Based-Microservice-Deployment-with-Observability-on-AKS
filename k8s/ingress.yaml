apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mk-simple-app
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mk-simple-app-service
                port:
                  number: 80
